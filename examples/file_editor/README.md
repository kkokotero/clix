# file-editor

A small text file utility.

Try it:

```bash
./build/examples/file-editor write notes.txt --text "hello"
./build/examples/file-editor append notes.txt --text "\nworld"
./build/examples/file-editor replace notes.txt --from world --to clix
./build/examples/file-editor show notes.txt --line-numbers
```
